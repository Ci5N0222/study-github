repository (저장소) : git이 파일을 기록해두는 장소 (.git)

온라인 저장소를 사용하는 이유
1. 컴퓨터가 갑자기 고장나도 안심할 수 있다.
2. 협업 가능

## `git push -u 저장주소 main`

## `git remote add origin 저장주소`
- 저장주소를 origin이라는 변수에 할당
- `git push -u origin main`
- 한번이라도 -u를 사용하면 `git push` 만으로도 push 가능